import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingTop: 35,
    flex: 1,
    justifyContent: "flex-start",
    alignItems: "center",
  },
  image: {
    width: 150,
  },
  textContainer: {
    marginTop: 30,
  },
  emptyText: {
    color: "#808080",
    fontSize: 16,
    textAlign: "center",
  },
  boldText: {
    color: "#808080",
    fontSize: 16,
    fontWeight: "bold",
    textAlign: "center",
  },
});
